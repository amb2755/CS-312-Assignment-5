import React from 'react'; // Import React to use JSX

// Define the Header component
function Header() {
    return (
        <header className="header">
            {/* Display the title "Keeper" */}
            <h1>Keeper</h1>
        </header>
    );
}

export default Header; // Export the Header component as the default export
